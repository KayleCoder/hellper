# 参照资料构建

给要**学习某一季官方中文版**的会话看。第一季的成品在 `s1ref/`，照着做就行。

## 目标产物

参照库的价值全在 `notes/` 里的三份整理，原始 OCR 只是中间产物：

| 文件 | 内容 | 给谁用 |
|---|---|---|
| `notes/glossary.md` | 人名/组织/术语总表，按登场顺序分段，标注首见话号与身份 | 翻译时查名词 |
| `notes/episodes.md` | 逐话一句摘要，按原作篇章标记分段 | 确认剧情脉络、找某个设定在哪话交代 |
| `notes/名詞對齊清單.md` | ★ 与当前 `CLAUDE.md` 术语表的**冲突项**和**待决项** | 交接时第一个看 |
| `notes/README.md` | 数据来源、文件说明、篇章结构表 | 索引 |

**第三份最重要**。它不是罗列，而是明确指出「你现在用的 X 应该改成 Y，依据是第几话」，
并说明影响到哪些 `zh.tsv`。第一季那份就抓出了两个错译（乔尔迪→裘勒迪、帕祖祖→帕朱朱），
已按它回改并重出了 190/192 的 PDF。第二季那轮又抓出六条（详见 `翻译知识库.md` §0），
其中「张光南→张光男」影响主角名。

## 现状：两季都已完成（2026-09-02）

| 季 | 原图 | OCR 文本 | notes/ | 状态 |
|---|---|---|---|---|
| 第一季（台版繁体，188話）| `s1ref/img/` 7,811 张 | `s1ref/ocr/` | `s1ref/notes/` | ✅ 完成 |
| 第二季（简体汉化，417集·完结）| `Session2/` 30,566 张 | `s2ocr/` | `s2notes/` | ✅ 完成 |

第二季的整理落在 **`s2notes/`**（不是原先设想的 `s2ocr/notes/`），四份文件与
`s1ref/notes/` 同结构。两季的结论已合并成 **`翻译知识库.md`**——查名词先看那份。

语料清单、OCR 工具链、踩过的坑见 **`语料与工具.md`**。

> 早期版本这里写着「s2ocr 有进程在写，别动」——**该警告已失效**，OCR 早就跑完了。

## 工具

- **`s1ref/ocrbin`** — macOS Vision OCR，可直接复用。
  用法：`./s1ref/ocrbin <放大倍率> <图片...>`，第二季用的是 `1.0`。
  ⚠️ 和 `pipeline/ocrtool2` **不是一个东西**：后者是韩文版（ko-KR），
  给韩文生肉用；识别中文官译要用 `ocrbin`。
- **`pipeline/s2ocr.sh` / `pipeline/s2ocr_rest.sh`** — 第二季的 OCR 驱动，6 路并行、跳过已完成、进度写 `s2ocr/progress.log`。
  换一季照抄改路径即可。
- **`pipeline/s2clean.py`** — 水印与噪声清洗。**通读整季前必过一道**，否则 token 翻倍。
  用法与原理见 `语料与工具.md` 第四节。
- **`s1ref/run.py`** — 第一季用的抓取+OCR 一体程序（走本地代理 `127.0.0.1:7897`、
  带 Referer 和真实 UA、失败指数退避、断点续传、话间随机间隔）。
  它已经踩平了图片 CDN 直连不通和 403 的坑，别重写。

**简体还是繁体**：第一季参照库是台版繁体（zh-hant）。能拿到简体官方版就优先用简体；
只有繁体就落地时按 `翻译方法.md` 第二节做繁转简——注意**用词也要换**，不只是字形。

## 整理成 notes/

抓完 OCR 只是一堆散文本，真正费工的是整理。做法：

1. **分批读 OCR**，每 10~20 话一批，边读边往 `episodes.md` 追加一句话摘要。
   一句话的标准：**谁做了什么、导致什么**，不要写「本话精彩激烈」这种。
2. **遇到名词就登记**到 `glossary.md`，记下**首见话号**。漫画里的角色资料卡
   （画面上那种带年龄/身分/别名的卡片）是最可靠的来源，登记时标「卡」。
3. **注意原作画面上的篇章标记**（`PROLOGUE END`、`SEASON 1 / MADMAN`、`END`），
   用它们给 `episodes.md` 分段，比自己按剧情猜准得多。
4. 全部读完后，拿 `glossary.md` 逐条比对 `CLAUDE.md` 的术语表，
   把**不一致的**写进 `名詞對齊清單.md`，注明依据和影响范围。

**两条从第二季那轮学到的**：

- **名字常常是成套的。** 暗党三位大议员＝战争手／战争舌／战争棒（War＋身体部位），
  四位大天使的名字合起来拼成 LOVE。台版把前者全部音译，单看一个名字永远发现不了。
  **登记名词时，把同一阵营的摆在一起看一遍。**
- **先想中国古典再想音译。** 作品大量借用三国／西游（奉先＝吕布、孙悟空、牛魔王、
  赤兔、如意棒、芭蕉扇、丈八蛇矛、觔斗云）与水浒（鲁智深）。「봉선」当初就是被
  硬拼成了「凤仙」。

## 交接给翻译会话

`名詞對齊清單.md` 写完后：

1. 按它回改 `CLAUDE.md` 术语表
2. `grep` 出受影响的 `<话号>/zh.tsv` 并改掉
3. 重跑那几话的 `render_zh.py` 并重出 `out/*.pdf`
   （需要 `ocr_png/`；如果已清理，源 PDF 还在就重跑 `render_pages.py`）
4. 在术语表备注里写清依据话号，避免下次又改回去

第一季那轮就是这么走的：查出「乔尔迪→裘勒迪」「帕祖祖→帕朱朱」两处，
改了 `CLAUDE.md`、`s3/190/zh.tsv`、`s3/192/zh.tsv`，重出了这两话的 PDF。全程约十分钟。

## 使用边界

抓下来的是**官方译本**。它在本项目里只有一个用途：**核对专有名词**。

- ✅ 查名词、确认人物关系与篇章结构、确认自己对韩文的理解
- ❌ 把官译句子搬进 `zh.tsv`
- ❌ 把 `s1ref/` `Session2/` `s2ocr/` 的内容整理成对外分发的成品

`out/*.pdf` 里的每一句中文都应该是照着韩文原文自己写的，参照库只负责让名字叫对。
