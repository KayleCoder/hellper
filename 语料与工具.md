# 语料与工具

给要**在本 workspace 里检索剧情、核对译名、或再抓一季参照库**的会话看。
翻译流水线本身见 `CLAUDE.md`；译名结论见 `翻译知识库.md`。

---

## 一、语料清单

| 路径 | 规模 | 内容 | 能不能动 |
|---|---|---|---|
| `s1ref/img/ep001~ep188/` | 7,811 张 / 798M | 第一季台版繁体原图 | 只读 |
| `s1ref/ocr/ep001~188.txt` | 752K | 上者的 OCR 文本 | 只读 |
| `Session2/ep001~ep417/` | 30,566 张 / 4.7G | 第二季简体汉化原图（416 个目录，ep007/008 合刊）| 只读 |
| `s2ocr/ep001~417.txt` | 2.7M | 上者的 OCR 文本，图与图之间以 `---` 分隔 | 只读 |
| `s2pdf/` | 5.1G | 第二季逐集合成 PDF，阅读用 | 可重建 |
| `s2/` | 1.1G | ep332–417 的来源 PDF（公众号） | 只读 |
| `s2ko/` | 654M | Naver 官方**韩文**原版，仅 ep007/008/009 | 只读，最高价值 |
| `s2en/` | 778M | ep105 的英文源与制作产物 | 见其 README |
| `out/` | — | 成品中文版 PDF | 输出 |

**用户明确要求保留全部图片存档**（打算传百度网盘备份），任何情况下不要清理
`Session2/` `s1ref/img/` `s2pdf/`。

### 三条容易踩的陈述
- **`s2/missing.txt` 是过时的待办清单，不是损坏记录。**列的 51 话全部有完整 PDF，已逐个核过。
- **`s2ocr/progress.log` 已经跑完了。**早期文档里「OCR 进行中，别动那两个目录」的警告已失效。
- **`ep007` 与 `ep008` 是合刊**，所以 417 集只有 416 个目录。写完整性校验时要单独处理，
  否则会误报缺 ep008（我在这上面误报过两次）。

---

## 二、OCR 工具链

### 两个二进制，别用混

| 工具 | 语言配置 | 用途 |
|---|---|---|
| `s1ref/ocrbin` | zh-Hant / zh-Hans / en-US | 读**中文官译**（第一、二季参照库）|
| `pipeline/ocrtool2` | ko-KR / en-US | 读**韩文生肉**（第三季翻译流水线）|

`ocrbin` 用法：`./s1ref/ocrbin <放大倍率> <图片...>`，输出以 `===FILE:<路径>` 分隔。
底层是 macOS Vision `VNRecognizeTextRequest`，`.accurate` 级别，开语言矫正。
识别块按 Y 排序（Vision 原点在左下，故取 `1 - midY`），同一行内按 X 排序，
行间阈值 0.012。第二季整季用的倍率是 `1.0`。

### 并行

```bash
ls Session2/ep*/  | xargs -P 6 ...        # 6 路并行，见 pipeline/s2ocr.sh / pipeline/s2ocr_rest.sh
```
6 路是实测的甜点，再高会被 Vision 内部排队抵消。

### 从 PDF 无损取图

不要用渲染（会重新栅格化、掉画质），用 PyMuPDF 直接抽出内嵌的原始 JPEG：

```python
import pymupdf
d = pymupdf.open(src)
for pi in range(d.page_count):
    for x in d[pi].get_images(full=True):
        info = d.extract_image(x[0])          # info['image'] 是原始字节，info['ext'] 是扩展名
```
ep332–417 的 6,901 张就是这么取的，与源文件逐字节等价。

---

## 三、Vision 的三个盲区

1. **极端长宽比会被降采样到糊。** 690×9650 这种整话拼成一张的图，直接送进去只能读出
   零星几行，很多张返回 0 行。**对策：切成 1400px 高的片、留 120px 重叠，OCR 后按行去重。**
   ep105 就是这么救回来的（152 行 → 277 行）。
2. **读英文美术字远不如读韩文/中文准。** ep105 的英文源 1x 和 2x 都花成
   `COME AT VOLIVE GOT THE SAIE` 这种。**英文源的话数不要指望 OCR，逐页看图转录。**
3. **只框到词的一半。**「데굴」只框到「굴」、`PLAGUE FLAG` 只框到 `PLAGUE`。
   残留检测的阈值要放到「1 个字」，不能只查 ≥2 个字。

---

## 四、核对与检索技术

### 2 倍放大重 OCR —— 判断「这个写法是真的还是 OCR 错了」

```bash
./s1ref/ocrbin 2 Session2/ep391/060.jpeg
```
本项目一半以上的译名结论都是这么定的。**但要记住一条教训**：我曾用单点 2× 复核
「威東格」没复现，就断言它是误读——结果另一话稳定复现。**单点复核只能证实存在，
不能证伪存在。**要证伪必须扫全语料。

### 全语料词频 —— 判断「哪个写法是主流」

```bash
grep -oh "铁手表" s2ocr/*.txt | wc -l          # 数量
grep -n  "铁手表" s2ocr/*.txt                  # 出处
```
注意区分**铭牌**（`统称：` `称号：` `外号：` 开头的那种角色资料卡）与**对白**。
铭牌权威，但两者冲突时要摆出来让人决定，不要自己压掉——`名稱對照表.md` 末尾的
「用字浮动表」就是干这个的。

### 无字图段探测 —— 找没有对白的场景

OCR 只能找到「周围有台词」的东西。要定位纯画面的段落（打斗、回忆、亲密戏），
按图统计文字行数，找连续的空段，再看它前后的台词判断内容：

```python
blocks = open(f's2ocr/ep{n:03d}.txt').read().split('\n---\n')
counts = [len([x for x in b.split('\n') if x.strip()]) for b in blocks]
# 找 counts 里连续 ≥5 个 <=1 的区间，再读该区间前后各 4 块的文字
```
全季扫一遍只有个位数命中，噪声很低。**这是回答「某某情节在哪一集」最快的办法。**

### 水印清洗 —— `pipeline/s2clean.py`

第二季汉化组的水印出现 21,817 次，且被 OCR 打散成几百种变体，
精确匹配和邻接正则都拦不住。有效的做法是**字符集重叠度**：

```python
WMCHARS = set('我与與洪胖的日常…')      # 水印可能碎成的字
def is_wm(s):
    core = [c for c in s if not c.isspace()]
    return core and sum(c in WMCHARS for c in core) / len(core) >= 0.75
```
配合长度上限、纯拟声词正则、连续重复行去重，噪声降了 49%（1.65MB → 851K）。
用法：`python3 pipeline/s2clean.py s2ocr/ep351.txt ...`，输出以 `##### ep351` 分段、行内用 `|` 连接。
**通读整季就是靠它把 token 压下来的**，别直接读原始 OCR。

---

## 五、网络

本机走 Clash 类代理，**所有对外请求必须显式指定**，否则 TLS 握手直接挂起：

```bash
curl -x http://127.0.0.1:7897 ...
```
（DNS 返回 `198.18.0.x` 就是 fake-IP 的特征；`scutil --proxy` 可确认端口。）
Python 的 `urllib` 走这个代理会 `CERTIFICATE_VERIFY_FAILED`，**改用 curl 子进程**——
`s1ref/run.py` 里已经处理好了（带 Referer、真实 UA、指数退避、断点续传），别重写。

---

## 六、Shell 与环境的坑

- 用户的 zsh 配置会刷一堆 `setValueForKeyFakeAssocArray: command not found` 噪音，
  与本项目无关。介意就接 `2>&1 | grep -v FakeAssoc`。
- **zsh 的 glob 不匹配时会直接报错中止整条命令**（`no matches found`），
  不像 bash 那样把模式原样传下去。我曾因此把一个跑得好好的任务误判为「死了」。
  写状态检查时用 `ls dir/ | grep` 而不是 `ls dir/ep4[0-1][0-9].txt`。
- BSD grep 的 `\|` 交替不生效，要用 `grep -E`。
- `pkill -f "python3 run.py"` 匹配不到——实际进程名是 `/…/Python run.py`。按 PID 杀。
- **macOS TCC**：`~/Desktop` 下读取任何非本进程创建的文件都会 EPERM，关沙箱也没用
  （拦截在系统层）。所以本项目固定在 `~/Workspaces/` 下。

---

## 七、写完整性校验时

统计「哪些话已覆盖」的正则要同时吃掉三种写法，否则会误报：

- 单集：`- ep123 …`
- 连号（**注意是 en-dash 不是连字符**）：`- ep116–ep118 …`
- 合刊：`- ep007–008（合刊）…`

我在这上面连续误报过两次（一次报 86 个假缺口，一次报 1 个）。
**报缺口之前先把命中的行打印出来看一眼。**
