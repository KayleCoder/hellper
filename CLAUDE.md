# HELLPER 韩译中流水线

把《HELLPER》（헬퍼）第三季的韩文原版 PDF，逐格擦掉韩文、排上中文，输出中文版 PDF。

本 workspace 同时存着**三部的全部语料与成品**：第一季台版繁中、第二季简中汉化（含官方
韩文原版 007–009）、第三季我的译文。查剧情、核译名、再抓一季，都在这里，见下表。

## 先读哪份

| 你要做的事 | 看这份 |
|---|---|
| 翻一话新的 | 本文件（工程流水线）+ **`翻译方法.md`**（译文怎么定） |
| **查人名/招式/设定** | **`翻译知识库.md`**（两季通读后的总表，先看这份）|
| 查剧情：某情节在哪一集 | `s2notes/每集一句.md`（第二季 ep001–417 全）、`s1ref/notes/episodes.md`（第一季 1–187話）|
| 查第二季名册 | `s2notes/名稱對照表.md`（末尾有**用字浮动表**，两条待拍板）|
| 查名词官译（第一季台版原文） | `s1ref/notes/glossary.md`、`s1ref/notes/名詞對齊清單.md` |
| **在语料里检索 / 再抓一季** | **`语料与工具.md`**（语料清单、OCR 工具链、Vision 盲区、踩过的坑）|
| 学某一季官方中文版、建参照库 | `参照资料构建.md` |
| 第一季独有角色译名 | `s2notes/第一季独有角色清单.md`（53 条已定稿）；征集页面 `译名征集表.html` |
| **取成品 / 交付** | **`上传/`**：成品只此一份，三部各一个文件夹，三位补零，可直接上传 |
| **译名定案后回改已出片的话** | `pipeline/redo_names.py`（只重排受影响的页）|

**核心分工**：专有名词一律以官译为准（查参照库），对白句子照着韩文自己写。
不要把官译的句子搬进 `zh.tsv`。

## 目录

```
hellper/
├── CLAUDE.md              本文件：工程流水线 + 硬规矩 + 术语表
├── 翻译知识库.md          ★ 两季通读后的人名/招式/设定总表，查名词先看它
├── 翻译方法.md            译文怎么定：术语对齐、风格、拿不准时怎么办
├── 语料与工具.md          语料清单、OCR 工具链、Vision 盲区、检索技术、环境坑
├── 参照资料构建.md        怎么把某一季官方中文版做成参照库
├── 译名征集表.html        第一季独有角色译名征集页（单文件，可离线分发）
│
├── 上传/                  ★ 唯一的成品所在地，也是全部交付：三部各一个文件夹，三位补零
│   ├── 第一季/  000–187   188 个（000=預告）  繁中官译
│   ├── 第二季/  001–417   417 个              简中汉化；007/008/105 是我译的
│   └── 第三季/  188–193     6 个              我的译文
│
├── _原件保留/             007-008 合刊原件、105 汉化组原版、007/008 汉化组版、331.old
├── s1ref/                 第一季：OCR ocr/、笔记 notes/、清单 list/、抓取器 run.py
├── s2ocr/                 第二季 OCR 结果
├── s2/                    第二季抓取残留：urls.txt、all.txt、日志、ca_bundle.pem
├── s2notes/               第二季名册、每集一句、第一季独有角色清单
│
├── s3/                    第三季（本流水线的主战场）
│   ├── src/               ★ 源 PDF：188 189 192 193（190/191 的源已丢失）
│   ├── 188/ … 193/        每话工作数据：blocks.json zh.tsv render_zh.py erase.json 对照表
│   └── _backup_20260902/  译名订正前的 zh.tsv
│
└── pipeline/              全部可复用脚本（下详）
```

> **2026-09-02 清理过一次，删了 12G 中间产物**，删掉的都是能从成品或源重建的：
> `Session2/`（第二季原图 JPG）、`s1ref/img/`（第一季原图 JPG）、
> `s2pdf/` 与 `s2/*.pdf`（与 `上传/第二季` 逐字节相同的副本，已 504 个全部校验）、
> 各话的 `ocr_png/ out_png/ res2x/ residual/`、`_backup` 里的旧成品 PDF、
> 以及 `out/`（内容已并入 `上传/`）。
> `s2ko/`、`s2en/` 在这次清理之前就已经不在了。
> **后果**：成品只剩 `上传/` 一份，没有第二副本；第二季/第一季的逐页原图要重排得重抓。
> 第三季 188/189/192/193 可由 `s3/src/` 完整重建，190/191 仍只能从成品 PDF 抽页重擦。

### pipeline/ 里有什么

| 脚本 | 用途 |
|---|---|
| `render_pages.py` | 源 PDF → 逐页 PNG，自动裁左右白边 |
| `prep_from_jpg.py` | 逐页图 → ocr_png，缩放到 1170px 宽（对齐各话工作分辨率）|
| `reslice.py` | 上万像素的整话长条 → 按横向空白带切成页 |
| `ocrtool2` / `ocr2.swift` | 韩文 OCR（ko-KR + en-US）二进制与源码 |
| `cluster.py` | OCR 行 → 气泡块 `blocks.json` |
| `render_zh.template.py` | 擦原文 + 排中文（三个配置表留空，按话填）|
| `check_wrap.py` | 排版体检：模拟真实折行，揪孤字行与被拆散的标点 |
| `redo_names.py` | ★ 译名订正后只重排受影响的页，其余页原样保留 |
| `sweep.py` / `sheet.py` | 全书缩略图扫视 / 接触印相 |
| `safebox.py` / `grid.py` | 量准擦除框：彩色气泡内腔 / 灰阶线稿坐标网格 |
| `qc.py` | 墨迹流失等质检 |
| `naver_fetch.py` | Naver 图片清单 → 下载 → 合成 PDF |
| `mkpdf.py` / `mkpdf.sh` | 逐集图 → PDF（第二季批量合成用）|
| `s2ocr.sh` / `s2ocr_rest.sh` / `s2clean.py` | 第二季 OCR 驱动与水印清洗 |
| `wx_*.py` | 公众号抓取（第二季 ep332–417 的来源）|

## 流程

```bash
cd ~/Workspaces/hellper
mkdir -p s3/192 && cd s3/192
cp ../../pipeline/{ocrtool2,cluster.py,render_pages.py} .
cp ../../pipeline/render_zh.template.py render_zh.py
sed -i '' "s#SRC=.*#SRC='/Users/zhouzibo/Workspaces/hellper/s3/src/192.pdf'#" render_pages.py

python3 render_pages.py                      # → ocr_png/  (约 20 秒)
./ocrtool2 ocr_png/*.png > ocr_box.tsv       # 韩文 OCR   (约 15 秒)
python3 cluster.py > blocks_list.txt         # → blocks.json + 待译清单
#   ← 在这里翻译，写 zh.tsv（格式：块编号 \t 中文，| 表示换行）
python3 ../../pipeline/check_wrap.py         # 排版体检：孤字/断标点
python3 render_zh.py                         # → out_png/
#   ← 质检（见下）
#   ← 合成 PDF 到 ../../上传/第三季/<话号>.pdf
```

### 译名定案后，回改已经出片的话

不要整话重渲染——只重排译文真正变了的页，其余页原样保留：

```bash
python3 pipeline/redo_names.py --map 凤仙=奉先 开票人=开票者 -- s3/192 s3/193
```

它会改 `zh.tsv`（先存 `zh.tsv.bak`）、算出受影响的页、只渲染这些页、再拼回 `上传/` 里的成品。
底图现在各话都没有 `ocr_png/` 了（188/189/192/193 可用 `render_pages.py` 从 `s3/src/` 重建），
源图已丢失的话（190/191）就从成品 PDF 抽页，
**在旧中文上重擦重排**——成品的嵌入图像像素与 `blocks.json` 坐标系一致，实测六话精确匹配。
前提是新译名**不比旧的长**，否则擦除框会变大，脚本会就此告警。

shell 里每条命令建议接 `2>&1 | grep -v FakeAssoc`，用户的 zsh 配置会刷一堆
`setValueForKeyFakeAssocArray: command not found` 噪音，与本流程无关。

## 翻译什么、不翻译什么

**翻译**：对白、旁白、铭牌（人物名/阵营/职位）、招式与标题带。

**保留原文**：所有拟声字（拟声词是画进画面里的美术字，擦掉必然伤画），以及占满画格的
巨型艺术字。这条是 190 的教训——见下。

## 硬规矩（九条，都是踩出来的）

### 1. 巨型拟声字一律不动

190 第一版把整页大的拟声字也擦了，后果：
- 037 页整格被抹白，墨迹流失 **57.4%**
- 067 页连人物手上的折纸翅膀一起删掉了
- 072 页白框啃进人群画面

判据：block 面积 > 约 15 万 px²、内容是纯拟声词 → 不写进 zh.tsv，原样保留。
修复后 190 的最大墨迹流失降到 8.7%。

**质检手段**（每话必跑）：把 `ocr_png` 和 `out_png` 缩到 94×253 比较暗像素数量，
流失超过 12% 的页必须人眼复核。注意这招测不出「红底白字」被填掉的情况（暗像素数不变），
所以还要看缩略图。

### 2. 气泡和特效字被聚成一块时必须手工拆开

cluster.py 会把靠近的对白气泡和大特效字合成一个 block，直接擦会毁画面。
用 `EXTRA` 手工拆：把该 block 从 zh.tsv 里去掉，在 EXTRA 里按真实位置补两个小 block。

EXTRA 的 key 规则：新块的编号接在该页原有块之后。例如 092 页原有 5 块（092.0～092.4），
则 `EXTRA['092']` 的第一项是 `092.5`、第二项是 `092.6`。

191 拆了 4 处（022 / 053 / 081 / 092），190 拆了 1 处（074）。

### 3. 拟声字要么整页都译，要么整页都不译

Vision 读不出低对比度的拟声字（白字压在紫色/黑色上），结果同一页出现「滋啦」和
「파직」混排，看起来像 bug。191 的 100/101/102/106 就是这样，最后把这 8 页的
纯拟声字统一还原成韩文。

### 4. EXTRA 块会自动加 24px 边距

`ex = 块框 ∪ 新文字范围 + pad`，pad 最大 24px。文字靠近画格边框或艺术时，
这 24px 会吃掉边框（192 的 016 页左边框就这么没的）。
**对策**：同时写一条同名的 `ERASE_OVERRIDE`——它会绕开 pad，精确按给定矩形擦除。

### 5. 衣物/道具上的字是画面，不是对白

192 的 073/075 把头带上的「노력」当对白翻了，结果糊了一块橄榄色方块盖住人物脸。
判据：字带着透视变形、跟着物体走 → 是画面，保留。

### 6. 原图可能已经自带汉字

193 的 081 页招牌上「흑관산」下面本来就写着「黑棺山」，再译一遍就成了重复。
遇到韩汉并置的招牌/标题，直接保留原文。

### 7. 源文件里可能混着已翻译的页

188.pdf 的 018 页是韩文、019 页是同一页的**中文版**（整本只此一处重复）。
Vision 只配了 ko-KR + en-US，会把中文误读成韩文，于是给 019 生成了假 block，
翻译后覆盖掉了原有的中文句子。
**对策**：开工前用缩略图签名比对相邻页，找出近似重复对；
中文页一律不碰。检测代码见本节末尾的思路（32×80 灰度签名，平均差 < 12 视为重复）。

### 8. 译文里不要夹韩文原词——字体没有谚文字形

排版字体 Hiragino Sans GB **不含谚文**，`zh.tsv` 里只要出现韩文就渲染成豆腐块。
S2_105 的译注写成「「哥哥」（오빠）一词兼有…」，成品里那两个字直接是 ⊠⊠。
要提原词就用中文描述，或干脆不提。上线前扫一遍：

```bash
grep -nP '[\x{AC00}-\x{D7AF}\x{3040}-\x{30FF}]' <话号>/zh.tsv
```

### 9. 窄高气泡要按框宽写断行，别照语义断

韩文竖排窄栏气泡（如 286x501）在 1170px 工作分辨率下每行只放得下 4~5 个汉字。
按语义写的 `|` 断行会溢出后被二次折行，挤出「刀」「间」「震」这种孤字。
**每次改完 zh.tsv 都跑 `pipeline/check_wrap.py`**，它模拟真实折行并标出孤字行与
被拆散的标点。S2_007 靠它一次揪出 47 处。

## Vision OCR 的三个盲区

**盲区一：完全读不出的美术字。** 既不在 blocks 里，也不会被残留检测发现。两个办法挖：

1. **2 倍放大重 OCR**：把擦除后的页放大 2× 再跑一遍 ocrtool2。
   挖出过 190 的「초미세자폭비행단」（超微型自爆飞行团，592px 宽的番号牌）、
   191 的 034 页整句对白「전부 뚫어 주마!!!!」。
2. **彩色标签扫描**：扫描饱和度高的墨迹区域，与 blocks 求差集。
   挖出过 191 的「FULL 모자이크」。噪声很大（画面本身就大量紫色），需人眼筛。

**盲区三：不能直接 OCR 成品来查残留。** Vision 只配了 ko-KR + en-US，会把渲染出的
中文误读成韩文——S2_007 这么查报出 96 处假「残留韩文」（「得救了!!!」被读成 `1묵#]!!!`）。
**正确做法：在原图上把擦除框涂白再 OCR**，图里没有中文就不会误读。
（`render_zh.py` 末尾加一行 `json.dump(erase_log, open('erase_log.json','w'))` 导出擦除框。）

英文源的话数则相反——扫成品里的**拉丁文字**很有效：S2_105 靠它逮到一个
1x/2x OCR 与缩略图扫视**都漏掉**的对白气泡。

**最有效的检测手段是全书缩略图扫视**（`pipeline/sweep.py`）。192 的 016 页
「전군, 공격―」和 054 页「먹겠습니다~!!!」「정의다!!!」三处整句对白，1x 和 2x OCR
都没读出来，只有肉眼扫缩略图才发现。**渲染完第一版就先扫一遍**，别留到最后。

**盲区二：只框到词的一半。** 190 的 088 页「데굴」只框到「굴」，输出成了「데滚」。
045 页的副标题「PLAGUE FLAG」只框到 PLAGUE，剩个孤立的 FLAG。
对策：残留检测要把「1 个韩文字」也算进去，不能只查 ≥2 个字。

## 量准擦除框

`pipeline/safebox.py <页号> y0 y1 中心y [中心x]` —— 逐行求「包含中心、且不含彩色像素」
的最大区间再取交集，得到彩色气泡内腔的安全擦除框。192 的尖角气泡靠它一次量准
（此前手工估了三轮都在啃气泡轮廓）。

`pipeline/grid.py <输出> <页号> x0 y0 x1 y1 [步长]` —— 带坐标网格的裁切图，
用来读取黑白线稿区域的坐标（safebox 对纯灰阶无效）。

填充色也要留意：黑底白字的横幅如果背后有光束/渐变，任何纯色填充都会截断它
（192 的 019 烧烤横幅就是，最后保留了原文）。

**彩色画面上的黑色艺术字用 `MASK_ERASE`**：它只重绘近灰阶像素（|max-min|<46），
彩色一律不动。189 的 004/005 两处压在红色喷溅上的大字，用矩形擦除会毁掉整片红色，
改用 MASK_ERASE 后红色喷溅完好无损、韩文干净消失。前提是同名要有一条 ERASE_OVERRIDE。
注意：框内如果有黑白线稿，也会被一起抹掉。

## 排版

- 字体：`/System/Library/Fonts/Hiragino Sans GB.ttc`，index=1（W6）
- `FONT_MAX`：默认字号上限 60px，对标题带来说**太小**，必须逐条放开
  （190/191 各放开了 14～22 条）。放开后要检查擦除框有没有戳出画格边框——
  190 的 055 番号牌就把边框左右两条竖线吃掉了，收窄框宽即可。
- `zh.tsv` 里用 `|` 表示换行。原文是两行的标题带，中文也要拆两行，
  否则会被压成一行显得又小又挤。

## 质检清单（每话）

1. 残留韩文 = 0（擦除区涂白后重新 OCR，排除有意保留的块）
2. 2 倍放大重 OCR，确认没有新发现的对白
3. 墨迹流失 > 12% 的页人眼复核
4. 全书缩略图扫一遍（找整页抹白、中韩混排）
5. 成品 PDF 反渲染抽查几页，确认页数与源文件一致

190 修复后与 191 都做到了**残留 0 处**。

## 术语表

> 2026-09-02：장광남 / 개표자 / 육두패 / 슈빌 / 존나나 / 봉선 六条已按用户确认订正。
> 作品大量借用**三国演义与西游记**人物（奉先＝吕布、美猴王孙悟空、赤兔、如意棒、芭蕉扇、
> 丈八蛇矛、觔斗云），遇到疑似人名先往这两部书上想，别当成韩语音译硬拼。
>
> **两组名字是成套的，必须整组一起译，不能拆开音译：**
> - **暗党三位大议员＝War＋身体部位**：战争手（워핸드）／战争舌（워텅）／战争棒（워배트），
>   对应手、舌、男性生殖器。台版全部音译成了「威漢德／威東格／沃貝特」，
>   十二次、六次、四次都没译出本义，**第三季一律改用意译**。
>   三人的名字（슈빌／존나나／샹）则都是韩语脏话谐音，台版第169話译者注已点明。
> - **四位大天使（明党大议员）＝L·O·V·E**，合起来拼成 LOVE，**写拉丁字母，不音译**。

| 韩文 | 中文 | 备注 |
|---|---|---|
| 상계 / 하계 | 上界 / 下界 | |
| 명당 / 암당 | 明党 / 暗党 | |
| 중의원 / 대의원 | 中议员 / 大议员 | |
| 비블리온 | 比布利翁 | 上界国宝，卷轴 |
| 슈빌·워핸드 | **修宾·战争手** | 暗党大议员「下面那位」。반·워핸드 =【半·战争手】。第二季 ep347「战争手的72分身中第9分身」、ep417「我是战争手」即此人 |
| 샹·워배트 | **香·战争棒** | 暗党大议员。배트 在韩语里只指球棒；蝙蝠是 박쥐 |
| 존나나·워텅 | **裘纳纳·战争舌** | 暗党大议员 |
| 엘 / 오 / 브이 / 이 | **L / O / V / E** | 四位大天使＝明党大议员。四个名字合起来拼成 **LOVE**，所以一律写拉丁字母，**不要音译成「艾尔／奥／伊」** |
| 오파님 | 奥法尼姆 | Ophanim，布满眼睛的球体战车 |
| 레드 / 블랙 / 핑.화이트 / 데드.일 / 폴더.레드 | 雷德 / 布莱克 / 平·怀特 / 戴德·佩尔 / 福德·雷德 | 明党中议员 |
| 파주주 | 帕朱朱 | 暗党辅佐官。台版官译，曾误作「帕祖祖」已回改 |
| 죠르디 | 裘勒迪 | [无业]。台版官译（第一部第52话标题），曾误作「乔尔迪」已回改 |
| 염 헬 2세 | 阎·赫尔二世 | 下界王 |
| 슬래셔 | 斯莱瑟 | 典狱长 |
| 사신 / 사자 | 死神 / 使者 | 两个词都出现，分开译 |
| 여의봉 | 如意棒 | |
| 파초선 | 芭蕉扇 | |
| 종이접기 | 折纸 | 招式系列：战斗机 / 轰炸机 / 圣域穹顶(홀리돔) |
| 동시접기 | 同时折 | 分级：<中级> <高级> |
| 홀리클라우드 | 圣云 | 上界之门 |
| 롱슬리퍼 | 长眠者 | |
| 아킬레스힐 | 阿喀琉斯之踵 | 外变形 |
| 신궁 | 神弓 | 全力变形 |
| 분신술 | 分身术 | 72体，战争手的能力 |
| 권능 | 权能 | |
| 망상연합 | 妄想联合 | |
| 샹 | 尚 | 角色的口头禅脏话，音译。与大议员「香·战争棒」同字，作者有意为之 |
| 아담 / 이브 / 에덴 | 亚当 / 夏娃 / 伊甸 | |
| 육두패 / 육두촌 | 肉头牌 / 肉头村 | 육두=肉头。甲乙丙丁洞队长：兰波/贤宇/仁者/猛奇 |
| 삼신 | 三神 | 中村之主 |
| 봉선 / 지룡(맥) / 세세 | 奉先 / 地龙(脉) / 世世 | 持「死亡票」者。奉先＝吕布字奉先，作品大量借用三国/西游人物 |
| 데스티켓 | 死亡票 | |
| 99단 | 99团 | |
| 장광남 | 张光男 | **开票者**（身分名「张狂男」，光→狂谐音，不是错字）|
| 묠니르 | 妙尔尼尔 | Mjölnir，奥法尼姆的招式 |
| 천벌 | 天罚 | 「霹雳」「雷雨」 |
| 장팔사모 | 丈八蛇矛 | 兵器召唤 |
| 흑관산 | 黑棺山 | 原图招牌已自带汉字，不必译 |
| 적토 | 赤兔 | 坐骑 |
| 형 / 사인 | 形 / 死因 | 193 的核心设定 |

> 音译类名词**先查 `s1ref/notes/glossary.md`**，别凭读音自己拼——已经栽过两次。

常用拟声词（小尺寸的才译）：쿡쿡쿡→咕咕咕、꺄하하→咔哈哈、꼴까닥→咕咚、
부들→抖、펑→砰、슝→咻、빠직→青筋、파직→滋啦、벅→挠、데굴→咕噜。

## macOS 权限的坑

**为什么要搬到这个目录**：桌面属于 macOS 隐私保护（TCC）目录。会话跑到一半时
对桌面的授权失效了，表现为读取任何**不是本进程创建的**文件都返回 `EPERM`——
包括源 PDF。关掉 Claude Code 的沙箱也没用，因为拦截在系统层。
`~/Workspaces/` 不受此限制，所以固定在这里工作。

附带后果：桌面上的 `190_中文版.pdf`（由另一个会话创建）无法覆盖。
最新的 190 修复版在 `上传/第三季/190.pdf`。

## 各话规模

| 话 | 页数 | 块数 | 译出 | 备注 |
|---|---|---|---|---|
| 188 | 185 | 295 | 192 | 第三季开篇；源 PDF 里 018/019 是同一页的韩/中两版 |
| 189 | 84 | 90 | 40 | 动作戏，拟声字为主 |
| 190 | 95 | 149 | 113 | 已按新标准修复 |
| 191 | 110 | 205 | 148 | 与 190 有一页重叠（源 PDF 如此） |
| 192 | 87 | 135 | 98 | 六斗村篇开始；奥法尼姆「妙尔尼尔」 |
| 193 | 82 | 183 | 154 | 对白密集；99团、丈八蛇矛、黑棺山 |

第二季另译三话（非本流水线主线，源与方法都不同）：

| 话 | 页数 | 块数 | 译出 | 源 | 备注 |
|---|---|---|---|---|---|
| S2_007 | 46 | 233 | 120 | Naver 韩文原版 | 漫画房失忆篇；页 010 整页 OCR 零结果（源已不在本地）|
| S2_008 | 45 | 106 | 74 | Naver 韩文原版 | 同上后篇；018 页巨型手写字跨页，保留 |
| S2_105 | 52 | 153 | 95 | **英文汉化组**（转译）| 源已不在本地，质量有天花板 |
