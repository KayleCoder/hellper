# 翻译方法

配套文档：`CLAUDE.md`（图像流水线与工程规矩）、`参照资料构建.md`（怎么建某一季的参照库）。
本文只讲**译文本身怎么定**。

## 一、总原则：术语查表，句子自己写

参照库（`s1ref/notes/glossary.md` 等）的用途是**回答「这个名字/招式/组织叫什么」**，
不是拿来抄句子。

- **专有名词**——人名、组织、招式、道具、地名、职衔——**一律以官译为准**。
  这些是既定事实，自己另起译名会让读者对不上前作。
- **对白句子**——**看着韩文原文自己译**。参照库里的对应段落可以用来确认理解是否有偏差，
  但成句要自己写，语气按当前这话的语境来。

一句话：**名词对齐，句子原创。**

## 二、开工前：先对齐术语

翻新的一话之前，按顺序做：

1. 读 `CLAUDE.md` 末尾的术语表——这是当前项目已定的译名。
2. 读 `s1ref/notes/名詞對齊清單.md`——参照库整理者列出的**冲突项与待决项**。
3. 本话 `blocks_list.txt` 出来后，把里面的专有名词拿去参照库里搜：
   ```bash
   grep -n "帕朱朱\|裘勒迪" s1ref/notes/glossary.md
   grep -rn "죠르디" s1ref/ocr/          # 韩文搜不到，官译库是中文，用中文搜
   grep -rln "裘勒迪" s1ref/ocr/ | head  # 找到首见话号，再看上下文确认身份
   ```
4. 新出现、参照库里也没有的名词，自己定译名，**当场写进 `CLAUDE.md` 术语表**，
   并在术语表备注里写清依据（第几话、什么身份），下一话才不会又改一次。

### 官译优先级高于「我觉得更好」

已经吃过的亏：
- `죠르디` 我按音译写了「乔尔迪」，官译是**裘勒迪**（第一部第 52 话标题就是这个名字）
- `파주주` 我写了「帕祖祖」，官译是**帕朱朱**

两处都已回改，`s3/190/zh.tsv`、`s3/192/zh.tsv` 和术语表都更新了。教训：**音译类名词一定先查**，
不要凭读音自己拼。

### 繁转简不是简单换字

参照库是台版繁体。落地到简体时：

- 字形转换（臺→台、體→体）只是第一步
- **用词也要换**：台版的「資訊/影片/計程車」在简体语境里是「信息/视频/出租车」
- 但**专有名词不换**：「裘勒迪」不要因为「勒」字改成别的
- 语气词按大陆网文习惯：台版的「啦/囉/嘛」可酌情调整，别照搬

## 三、译文风格

从 188~193 六话摸出来的一套：

| 项目 | 做法 |
|---|---|
| 语域 | 帮派/热血漫，口语化。脏话用「可恶/混蛋」这一档，不升级也不消毒 |
| 省略号 | 原文 `..` 或 `...` 一律写成 `..`（两点），保持画面紧凑 |
| 感叹号 | 跟原文数量走，`!!` 就是 `!!`，不要减 |
| 括注 | 原文 `『』「』` 保留，用于招式名和引述 |
| 铭牌 | `名字\|阵营(界)\|[职衔]`，三行，和原图排布一致 |
| 换行 | `zh.tsv` 里用 `\|`。**原文两行的，中文也拆两行**，否则会被压成一行显得又小又挤 |
| 数字 | 保持阿拉伯数字（99团、死亡票41张、基斯12） |

### 几个反复出现的处理

- **`샹`**：角色口头禅式脏话，音译作「**尚**」。它同时也是角色名（尚·战争棒），
  两种用法都写「尚」，靠语境区分——原文本来就是这个双关。
- **拟声词**：小尺寸的才译（咕咕咕/咔哈哈/咕咚/砰/咻/青筋/滋啦）；
  大的一律保留原文，理由见 `CLAUDE.md` 第 1 条。
- **原图已有汉字的**：直接保留，别再译一遍。189 的「오장육부/분해」下面本来就印着
  「五臟六腑/分解」，193 的「흑관산」招牌旁边就是「黑棺山」。
- **拉丁字母**：`Shield`、`Anti-Cross`、`Layer 食 541548`、`SECOND BIGBANG` 这类
  原样保留，不用译。

## 四、看不懂的时候怎么办

OCR 出来的韩文经常缺字、糊字。按这个顺序处理：

1. **裁图看原文**——`python3 ../pipeline/sheet.py 输出.png 块编号 ...`，
   Vision 读错的字肉眼一看就明白（190 的「휴일콤」其实是「홀리돔」Holy Dome，
   192 的「플리트」其实是「묠니르」Mjölnir）。
2. **看上下文**——把前后两三页整页裁出来看，招式名往往在后一页有效果画面佐证。
   192 的「妙尔尼尔」就是靠整话的雷电主题（霹雳/雷雨/避雷针模式）确认的。
3. **查参照库**——前作出现过的，官译里有。
4. **仍然拿不准**——**保留原文**，别猜。猜错的名词会污染后续所有话。
   在 `<话号>_对照表.tsv` 的备注里标出来，交给下一轮处理。

## 五、交付前的术语自检

```bash
# 1) 本话新出现的专有名词，是否都进了 CLAUDE.md 术语表
# 2) 与既有译名是否一致（易错：同一个名字在不同话写法不同）
cd ~/Workspaces/hellper
for t in 修比尔 基斯12 帕朱朱 裘勒迪 尚 孙 三神 凤仙 张光南; do
  echo "== $t"; grep -l "$t" */zh.tsv 2>/dev/null | tr '\n' ' '; echo
done
```

任何一个名词只在某几话出现、别的话写法不同，就是漏改。

## 六、参照库的边界

`s1ref/` 里的 OCR 文本是**官方译本的抓取件**，只在本地做术语核对用。

- ✅ 查名词、确认人物关系、确认篇章结构
- ✅ 读某段确认自己对韩文的理解没跑偏
- ❌ 把官译句子直接搬进 `zh.tsv`
- ❌ 把 `s1ref/` 的内容整理成可分发的成品

成品 `out/*.pdf` 里的每一句中文都应该是照着韩文原文自己写的。
